import 'package:flutter/material.dart';

class AppColors {
  // Background Colors
  static const Color background = Color(0xFFF6F6F6);

  // Primary Colors
  static const Color primary = Color(0xFF6610F2);

  // Text Colors
  static const Color textPrimary = Color(0xFF030306);
  static const Color textTertiary = Color(0xFF999999);

  // -------------------------- Dark Theme Color ---------------------------- //

  // Background Colors
  static const Color darkBackground = Color(0xFF141A16);

  // Primary Colors
  static const Color darkPrimary = Color(0xFF81EF71);

  // Secondary Colors
  static const Color darkSecondary = Color(0xFF172622);
  static const Color darkCardBorder = Color(0xFF3F4440);

  // Text Colors
  static const Color darkTextPrimary = Color(0xFFE8EAE9);
  static Color darkTextTertiary = Color(0xFFBABEBD);

  // Error/Warning/Success
  static const Color success = Color(0xFF11BC0D);
  static const Color error = Color(0xFFFF394B);
  static const Color warning = Color(0xFFFFA000);

  // Utility
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color transparent = Colors.transparent;
}
